module second(clkin,sw0,key1,key0,high,low,out):
input clkin,sw0,key1,key0;
output reg cout;
output reg [3:0] high;
output reg[3:0] low;
always @ (posedge clkin)
	begin 
		if(sw0)//暂停状态
			begin
				low = low +1;
				if(low == 10)
					begin
						low = 0;
						high = high + 1;
					end
				else
				
			end
		
		else
			begin
				//sw0为0的状态

			end
		
	end
endmodule 